$mod52

ORG 0h

MAIN:
	MOV		A,P0
	MOV		R0,A
	RR		A
	MOV		R1,A
	RR		A
	MOV		R2,A
	RR		A
	MOV		R3,A

	MOV 	A, R2
	CPL 	A
	ANL		A, R3
	MOV		R4, A

	MOV		A, R1
	CPL		A
	ANL		A, R0

	ORL		A, R4

	RRC		A
	JC		RESULT1
	JMP		RESULT0

RESULT1:
	MOV P1, #0FFh
	JMP FINAL

RESULT0:
	MOV P1, #0h

FINAL:
	JMP MAIN

END